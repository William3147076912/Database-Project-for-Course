<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qwq.user.mapper.TeacherMapper">

    <resultMap type="Teacher" id="TeacherResult">
        <result property="userId"    column="user_id"    />
        <result property="userName"    column="user_name"    />
        <result property="courseCount" column="courseCount"/>
    </resultMap>
    <select id="teacherCount" parameterType="Long">
        select count(*) from sys_user natural join sys_user_role where role_id=101
    </select>
    <select id="listTeacher" resultMap="TeacherResult">
        select user_id,user_name,count(course_id) as courseCount from sys_user natural join sys_user_role left join course on sys_user.user_id=course.creator_id where role_id=101 or role_id=100 group by user_id
    </select>
</mapper>